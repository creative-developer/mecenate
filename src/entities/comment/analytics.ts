export const commentAnalytics = {
  trackCommentsViewed: (_params: { postId: string }) => {
    // Connect analytics provider in app layer when needed.
  },
  trackCommentAdded: (_params: { postId: string }) => {
    // Connect analytics provider in app layer when needed.
  },
};
